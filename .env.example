RTMP_BASE=rtmp://localhost:1935/live
HLS_BASE_URL=http://localhost:8080/hls
NGINX_STAT_URL=http://localhost:8080/stat
MONGODB_URI=mongodb://root:password@localhost:27017/
REDIS_BROKER_URL=redis://localhost:6379/0
REDIS_BACKEND_URL=redis://localhost:6379/0
FFMPEG_DOCKER_IMAGE=jrottenberg/ffmpeg:latest
CORS_ORIGINS="*"
UPLOAD_DIR="uploads"
OUTPUT_DIR="outputs"


# ─── Azure Blob Storage ───────────────────────────────────────────────
# Copy the FULL connection string from Azure Portal → Access keys → key1
AZURE_STORAGE_CONNECTION_STRING=
# The AccountKey value from INSIDE the connection string above — must be identical
AZURE_STORAGE_ACCOUNT_KEY=

AZURE_STORAGE_ACCOUNT_NAME=videostreamstorage
AZURE_CONTAINER_NAME=livewire

AZURE_CDN_URL=